import { ICONS } from '../../lib/Constants';
import { moduleChannelUpdate } from '../../lib/Utilities';

export default moduleChannelUpdate(
  'hallOfFameChannelUpdate',
  'Hall Of Fame',
  ICONS.TROPHY,
  'hall-of-fame',
  'hallOfFamePublish',
);
